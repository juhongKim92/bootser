# Backend Internals — TODO

> 이 폴더는 `public/` 밖이라 배포되지 않는다.
> `wrangler.toml` 의 `[assets] directory = "./public"` 이 업로드 범위를 정하므로,
> 여기 무엇을 넣든 사이트 URL 로는 접근되지 않는다.

## 콘텐츠

- [x] 03. 컨시스턴트 해싱 — 노드 하나가 빠지면 캐시는 어떻게 무너지는가
- [x] 04. Raft 리더 선출 — 네트워크가 갈라지면 누가 리더가 되는가

### 실시간 통신

- [x] 05. MQTT QoS 0/1/2 — 한 번만 전달한다는 약속의 값 (`/mqtt/`)
- [x] 06. WebSocket 다중 서버와 Redis Pub/Sub — 서버를 늘렸더니 메시지가 안 간다 (`/websocket/`)
- [x] 07. Keep-alive 와 죽은 커넥션 + LWT — 서버는 두 시간 동안 살아있다고 믿는다 (`/keepalive/`)

> 실시간 통신 시리즈 3편 완료 (05 · 06 · 07).

### 장애에서 나온 것들 (`docs/history/` 기반)

> 출처: 저장소 루트의 `docs/history/` 7개 문서 — 6개 저장소 · 약 2,500 커밋.
> 기존 01~04 가 교과서 개념이었다면 이쪽은 실제로 대가를 치른 것들이다.
>
> 선정 근거는 `prevention-automation.md` §1-1 의 **"몇 개 저장소에서 독립 재발했나"** 숫자.
> 6/6, 5/6 짜리는 나만 겪는 일이 아니라는 뜻이다.
>
> 실시간 통신 시리즈와 축이 겹치지 않는다(DB · 동시성 · 배치).

- [x] 08. **17밀리초 쿼리가 커넥션 풀을 말린다** — OSIV · 트랜잭션 경계 (`/connpool/`)
- [x] 09. **인스턴스를 2대로 늘리는 순간 같은 작업이 두 번 돈다** — 작업 선점 (`/jobclaim/`)
- [x] 10. **롤백해도 시퀀스는 돌아오지 않는다** — 재시도 루프 폭주 (`/retryloop/`)
- [x] 11. **거부는 버그가 아니라 신호다** — 백프레셔와 큐 정책 (`/backpressure/`)
- [x] 12. **응답을 순서로 맞추면 언제 어긋나는가** — correlationId (`/correlation/`)

> "장애에서 나온 것들" 5편 완료 (08 · 09 · 10 · 11 · 12).

### 옵티마이저와 계획 캐시

- [x] 13. **여섯 번째 실행부터 느려진다** — generic plan 과 파라미터 편향 (`/genericplan/`)

### 회복 불가 상태

- [x] 14. **부하는 걷혔는데 돌아오지 않는다** — 재시도 증폭과 메타스터블 (`/retrystorm/`)

> "용량은 왜 무너지는가" 3부작 완료 (08 · 11 · 14).

### DB 정확성

- [x] 15. **둘 다 확인했고 둘 다 통과했다** — 격리 수준과 write skew (`/writeskew/`)

> 인덱스에서는 "개념"이 아니라 **"조용히 틀리는 것들"** 그룹에 넣었다 —
> 그 그룹의 부제("예외도 로그도 없이 결과만 틀린다")가 이 페이지 문장 그대로다.

### 캐시

- [x] 16. **TTL 이 끝나는 순간** — 캐시 스탬피드 (`/stampede/`)

### 호출 사슬

- [x] 17. **각자 1초씩 줬는데 왜 아무도 1초를 못 쓰나** — 데드라인 전파 (`/timeout/`)

  > **착수 전 게이트 판정 (2026-08-13). 통과 — 단, 원래 가설은 조건부로만 성립한다.**
  >
  > 모델: 클라이언트 → A → B → C, 한 방향 홉 25ms. C 는 65ms 아니면 1.2초에 답한다
  > (두 점으로 줄여서 미터가 표본이 아니라 **모든 경로의 확률 가중 열거**가 된다 —
  > 최대 r²=9 호출이라 잎이 512개를 안 넘는다. 난수가 없어 수치가 정확히 재현된다).
  >
  > - *"재시도를 늘리면 성공률이 떨어진다"* — **시도 횟수만 올리면 안 떨어진다.**
  >   전부 1초에서 시도 1→3 회로 올려도 성공률은 90.0% → 90.0% 로 **소수점 한 자리도
  >   안 움직인다.** 대신 C 호출이 1.00 → 1.80 건, 늘어난 0.80 건은 **전부** 기한 뒤 시작.
  > - **떨어지는 건 "고정된 예산을 시도 횟수로 쪼갤 때" 이고, 느림이 그 요청에 지속될
  >   때다.** 3초를 1회로 쓰면 100.0%, 1초씩 3회로 쪼개면 90.0% — 재시도가 성공률을
  >   10.0%p 깎는다. 느림이 *일시적*이면 같은 설정에서 99.9% 라 반전이 뒤집힌다.
  >   그래서 "C 의 느림 = 지속적/일시적" 손잡이를 넣었다. 이게 없으면
  >   "재시도는 늘 나쁘다"로 과장된다.
  >
  > **원래 우려(반전이 2개뿐이라 얇다)는 해소됐다 — 4개가 됐고 한 개는 예상 밖이었다.**
  >
  > 1. 전부 같은 값이면 **안쪽 타임아웃은 구조적으로 발동하지 못한다.** 발동 시각이
  >    1000 / 1025 / 1050ms — 바깥이 항상 먼저 포기한다. 설정 파일에 있고 코드도
  >    도는데 값이 무엇이든 결과가 같다. 죽은 설정이다
  > 2. 시도 3배 → 성공률 0.0%p, C 호출 1→9 (느린 요청 하나 기준), 그중 8이 기한 뒤,
  >    동시 최대 5건, 마지막 작업이 5.28초에 끝난다 (1초를 준 요청인데)
  > 3. 예산을 쪼개면 100.0% → 90.0% (지속적) / 99.9% (일시적)
  > 4. **전파는 성공률을 안 바꾼다.** 750개 조합 전수 비교에서 올린 조합 0개,
  >    낮춘 조합 0개. 해석적으로도 같다 — 전파가 주는 예산
  >    `기한 − 보낸시각 − 1홉` 의 성공 조건이, 전파 없이 "쓸모 있으려면" 조건인
  >    `기한 − 보낸시각 − 3홉` 과 글자 그대로 같아진다. **자르는 게 어차피 버려질
  >    응답뿐이다.** 그래서 전파는 성공률 장치가 아니라 비용·정직성 장치다 —
  >    평균 C 호출 1.16 → 1.08, 기한 뒤 시작 0.06 → 0.00, 고아 응답 0.044 → 0.000,
  >    실패 확정이 3.05초 → 1.00초

  > **원안에서 덜어낸 것 (2026-08-12 판단) — 그대로 지켰다.**
  >
  > - ~~"포기한 뒤에도 일은 계속된다"~~ → 14 의 핵심 연출. 레시피 2 에서 한 번
  >   언급하고 14 로 링크만 걸었다
  > - ~~"타임아웃을 늘리면 점유가 늘어 용량이 준다"~~ → 08 의 리틀의 법칙.
  >   "예산을 올린다" 칸에서 한 줄 + 08 링크로만
  >
  > 축은 예산의 **정합성** 하나로 유지했다 — 예산 1초 · 꼬리 1.2초 · 시도 3회가
  > 동시에 성립할 수 없다는 표가 페이지의 결론이다.

  > **검증.** 배포되는 `timeout-lab.js` 를 DOM 스텁으로 그대로 구동해 본문이 인용한
  > 모든 수치를 대조했다 (jsdom 없이 — 실험대가 쓰는 DOM API 가 몇 개뿐이다).
  > 레시피 7개 · 시나리오 7단계 · 본문 인용값 전부 실측과 일치. ko/en i18n 키 집합이
  > 같고, 9개 극단 설정에서 `undefined` · 미치환 `$1` 이 남지 않는 것도 확인했다.
  > 검증은 `tools/verify-timeout.mjs` 에 단언 98건으로 고정했다.

### 분산 락

- [x] 18. **락을 잡았는데 둘이 들어왔다** — 락 만료와 펜싱 토큰 (`/lockttl/`)

  > **게이트 판정 (2026-08-13). 통과 — 09 에 흡수시키지 않고 페이지로 만들었다.**
  > "09 에 흡수시켜도 되는 크기" 로 봤었는데, 독립 반전이 4개 나와서 페이지가 됐다.
  >
  > 1. **아무것도 고장나지 않아도 둘이 들어온다.** 정지 확률 0%, 네트워크 정상,
  >    락 획득 성공 — 작업(5초)이 TTL(3초)보다 길기만 하면 중복 실행률 100.0%,
  >    같은 일 2.00회, 락 없이 일한 시간 4000ms. 만료는 락 서버가 조용히 하는 일이라
  >    당사자는 알 방법이 없다
  > 2. **펜싱 토큰은 중복 실행을 한 자리도 못 줄인다.** 같은 일 2.00 → 2.00회,
  >    중복 실행률 100.0% → 100.0%. 바뀌는 건 낡은 쓰기가 최종이 되는 비율
  >    16.0% → 0.0% (거부된 쓰기 0.16건) 하나뿐이다. **펜싱이 산 것은 정확성이고
  >    산 적이 없는 것은 상호배제다** — 이게 페이지의 축
  > 3. **낡은 쓰기가 이기는 조건은 정확히 "정지 > TTL".** 3000ms 에서 0.0%,
  >    3100ms 에서 16.0% 절벽
  > 4. **watchdog 이 막을 수 있는 정지의 상한은 TTL 그 자체다.** 갱신 간격을
  >    1000 → 100ms 로 줄이면 2.5초 정지는 20.0% → 0.0% 로 막지만, 4초 정지는
  >    20.0% 그대로. 여유가 `TTL − 갱신 간격` 이라 간격을 0 으로 줘도 TTL 을 못 넘는다.
  >    정지 중에는 갱신 스레드도 멈춰 있다
  >
  > **TTL 에 좋은 값이 없다** — 중복 0% 이려면 TTL ≥ 작업 + 최대 정지 = 9초이고,
  > 그 값이 곧 죽은 워커가 잡은 락을 기다리는 시간이다. 이 표가 페이지의 결론이다.
  >
  > 겹침 점검 — 09 는 락을 *제대로 잡는* 방법(만료를 안 다룬다), 16 은 프로세스 안
  > 싱글플라이트이고 "락 TTL 이 재계산보다 짧을 때" 를 예고만 해뒀다. 18 의 고유 재료는
  > 제대로 잡은 락이 일하는 중에 사라지는 것 · 갱신의 상한 · 정확성이 자원 쪽에서
  > 나온다는 것이다.
  >
  > **Redlock 은 이 문제를 안 건드린다**는 절을 넣었다. 그래서 손잡이에 락 서버 대수가
  > 없고, 없는 이유를 본문에 적었다. 펜싱이 불가능한 자원(평범한 `UPDATE`, 외부 API)에서는
  > 멱등성으로 옮기는 게 답이고, 그건 05 의 QoS 1 결론과 같은 자리다.
  >
  > 검증은 `tools/verify-lockttl.mjs` 에 단언 73건으로 고정했다.

### 네트워크 한도

- [x] 19. **대역폭은 남는데 처리량이 안 나온다** — BDP 와 제곱근 법칙 (`/throughput/`)

  > **게이트 판정 (2026-08-13). 통과.** 원래 평가는 "묶는 페이지로는 가치가 있으나
  > **신규 정보는 적다**" 였다. 그래서 게이트를 "05·08 의 재진술이 아닌 반전이 2개
  > 이상 나오나" 로 세웠고, 3개가 나왔다.
  >
  > 모델은 세 한도의 `min` 이다 — 대역폭 · `N·W/RTT` · `N·MSS·√1.5/(RTT·√p)`.
  > 난수가 없어 화면의 모든 수치가 정확히 재현된다.
  >
  > 1. **대역폭을 1000배 올려도 처리량이 한 자리도 안 움직인다.** 1→10 Gbps 에서
  >    5.24 Mbps 그대로이고 활용률만 0.524% → 0.052% 로 나빠진다. 곡선이 꺾인 뒤로는
  >    평평하다. 05·08 에 없는 재료다 (BDP)
  > 2. **손실 0.00207% 위에서 커넥션 6개짜리 HTTP/1.1 이 HTTP/2 를 앞지른다.**
  >    손실 한도가 커넥션 수에 정비례하기 때문이다. 손실 0.1% 에서 27.14 vs 4.52 Mbps.
  >    교차점이 실제 인터넷 경로(0.01~1%)보다 훨씬 아래라는 게 핵심이다
  > 3. **HTTP/3 는 이 법칙을 안 고친다.** QUIC 도 혼잡 제어는 커넥션 단위라 처리량이
  >    HTTP/2 와 정확히 같다. 고치는 것은 전송 계층 HOL 이고, 그건 처리량이 아니라
  >    개별 스트림 완료 시각에 나타난다 — 흔한 오해라 절을 따로 뒀다
  >
  > **묶는 값도 살렸다** — 리틀의 법칙이 이 사이트에 세 번째로 나온다.
  > 이 페이지는 바이트(창×커넥션), 08 은 커넥션 풀 크기, 05 는 Receive Maximum.
  > 단위만 다르고 식은 같다. 기본 설정에서 1Gbps 를 채우려면 12.5MB 를 띄워야 하는데
  > 64KB 를 띄우고 있다 (191배).
  >
  > **정직성 한 줄** — 제곱근 법칙은 손실 기반 혼잡 제어(Reno·CUBIC)의 성질이고
  > **BBR 은 여기서 벗어난다.** "현장에서" 표에 밝혀뒀다. 슬로 스타트 구간을
  > 다루지 않는다는 것(정상 상태만 본다)도 함께 적었다.
  >
  > 검증은 `tools/verify-throughput.mjs` 에 단언 50건. **본문 오류 1건을 이 스크립트가
  > 잡았다** — 12.5MB ÷ 64KB 를 "512분의 1" 로 써뒀는데 실제로는 191 이다.

### 메시지 소비

- [x] 20. **컨슈머는 쉬지 않고 도는데 lag 는 줄지 않는다** — 리밸런싱 루프 (`/rebalance/`)

  > **게이트 판정 (2026-08-13). 통과.** 보류 이유가 둘이었다.
  > "앞의 것들을 다 쓴 뒤로" 는 17·18·19 가 나오면서 풀렸고,
  > **"프로덕션 경험선 밖"** 이 남아서 게이트를 이렇게 세웠다 —
  > *경험담 없이 메커니즘만으로 정확히 계산되는 반전이 2개 이상 나오나.* 5개 나왔다.
  >
  > 1. **유효 처리량이 정확히 0 인데 컨슈머는 98.4% 바쁘다.** 500건 × 700ms = 350초가
  >    `max.poll.interval` 300초를 넘으면 428건을 처리하고 전부 버린다 —
  >    auto-commit 은 `poll()` 안에서 일어나는데 거기로 못 돌아갔으니까.
  >    0 에 가까운 게 아니라 정확히 0 이다
  > 2. **원인이 피드백이 아니라 부등식이다.** `records × 처리시간 > interval` 이 참이면
  >    매 배치마다 확정적으로 쫓겨난다. 부하와 무관하고 컨슈머 1대여도 그렇다.
  >    14 의 메타스터블과 다른 점 — 부하를 걷어도 안 나온다. 나가는 문이 설정값에만 있다
  > 3. **컨슈머를 12배로 늘려도 처리량은 0 그대로, 낭비만 12배**(4.21 → 16.84 rec/s).
  >    lag 를 보고 스케일아웃하는 가장 자연스러운 반응이 아무것도 못 한다
  > 4. **같은 부등식의 두 변수인데 대가가 다르다.** `interval` 을 1시간으로 올리면
  >    처리량이 돌아오지만(4.29) 장애 감지가 3600초로 늘어난다. `records` 를 100 으로
  >    줄이면 처리량은 똑같은데 감지 지연은 300초 그대로다
  > 5. **cooperative-sticky 가 고치는 것은 리밸런싱의 폭발 반경뿐이다.** 멤버 변동
  >    6회/분에서 2.14 → 3.57 (+66.7%). 그런데 부등식을 넘으면 eager 와 똑같이 0 이다.
  >    17 의 "전파는 성공률을 안 바꾼다", 18 의 "펜싱은 중복 실행을 안 줄인다" 와 같은 형태
  >
  > **경험선 문제는 이렇게 처리했다** — 경험담을 쓰지 않았고, **"모델링하지 않은 것"** 절을
  > 따로 뒀다(KIP-848 · 파티션 편중 · 처리시간 분포의 꼬리 · 재처리의 2차 효과).
  > 인덱스 그룹도 "장애에서 나온 것들" 이 아니라 **용량**에 넣었다.
  >
  > 헷갈리는 지점 하나를 표로 박아뒀다 — `session.timeout.ms` 는 백그라운드 스레드가
  > 보내는 하트비트로 재므로 긴 처리에 **안 걸린다.** 걸리는 것은 `max.poll.interval.ms`
  > 하나다. 그래서 "컨슈머는 살아 있는데 그룹에서 빠졌다" 가 된다.
  >
  > 검증은 `tools/verify-rebalance.mjs` 에 단언 54건.

### 다음 후보 — `docs/history/` 재탐색 (2026-08-13)

> 20편으로 기존 목록이 비어서 저장소 루트의 `docs/history/` 8개 문서(3,164줄)를 다시 훑었다.
> `prevention-automation.md` §2 의 결함 유형 A~J 를 20편과 대조한 결과 —
> C·D·E 는 이미 썼고(10·14 / 09·18 / 08), J 는 **소재에서 완전 제외**(아래 규칙),
> H 는 메커니즘이 아니라 태도(대증요법)라 11·14 에 이미 녹아 있다.
> 남은 것이 A·B·F·G·I 이고, 그중 실험대가 나오는 것만 골라 아래 4개로 정리했다.
>
> **아래를 페이지로 옮길 때는 반드시 익명화한다** — 저장소명 · 커밋 해시 · 실제 테이블·컬럼명 ·
> 펌웨어/제품 식별자 · 서버 경로 · 내부 API 경로가 원문에 그대로 있다.

- [ ] **21. FIN 이 데이터를 앞질러 도착한다** — 정상 종료가 유실을 만든다
  - 출처: `event-stream-history.md` §1-3 (문서가 ★ 표시). 유형 F 계열
  - 클라이언트가 전송 직후 소켓을 닫는데, **TLS 종단이 로드밸런서에 있으면** 종단 장비가
    복호화한 데이터를 백엔드로 다 넘기기 전에 FIN 이 먼저 도달해 뒷부분이 버려진다
  - 반전 후보 (착수 전 수치 확인 필요)
    - **서버에서 기다리는 우회책이 완전히 무효다** — 버리는 주체가 서버 쪽이 아니다.
      실제로 그 우회책을 넣었다가 삭제했다
    - 해법이 코드가 아니라 **프로토콜 합의**다 (서버가 종료 신호를 에코하고, 클라이언트는
      그 응답을 받은 뒤에 닫는다)
    - 그 에코를 구현하면서 **`writeAndFlush` 가 비동기라 flush 완료 전에 닫아 같은 버그를
      서버가 재현**했다. 같은 실수가 양쪽에서 한 번씩 난다
  - 손잡이: 전송량 · 종단 장비의 전달 지연 · 클라이언트 close 시점 · 종료 신호 에코 on/off
  - 07 과 축이 겹치지 않는다 — 07 은 "안 끊긴 걸 못 알아챈다", 이쪽은 "정상 종료가 유실을 만든다"
  - **추천 1순위.** ★ 항목이고 반전이 세 겹이며, "그 처방이 고치는 것은 당신이 생각하는
    그것이 아니다" 라는 17·18·20 의 논지에 그대로 얹힌다

- [ ] **22. 연속된 1을 몇 번으로 세나** — 샘플링 주기가 집계를 부풀린다
  - 출처: `bsd-history.md` §4-3. 유형 G 계열
  - 상태 비트가 켜진 **모든 샘플**을 세면 한 번의 사건이 샘플링 주기만큼 중복 집계된다.
    `0→1` 전환만 세야 맞다
  - 반전 후보
    - 부풀림 배수가 **샘플링 주기에 정비례**한다 — 센서를 촘촘하게 하면 통계가 **더** 틀린다
    - 그런데 어떤 지표는 행별 합산이 맞다(한 행에서 여러 항목 동시 감지 — 같은 문서 §4-4).
      **같은 데이터에 두 집계 규칙이 동시에 옳다**
  - 손잡이: 샘플링 주기 · 사건 지속시간 · 사건 간격 · 집계 규칙(행별 합산 / 전환 카운트)
  - 배수가 정확히 계산되고 절벽이 선명하다. 겹치는 페이지가 없다. **추천 2순위**

- [ ] **23. 같은 숫자가 두 화면에서 다르다** — 집계 경로 난립
  - 출처: `bsd-history.md` §4-5 · §4-6, `vwc-history.md` §4.2 · §4.4. 유형 G 계열
  - 한 지표를 원천 재집계 경로와 저장값 경로가 각자 계산해 두 API 값이 갈린다
  - 반전 후보
    - **둘 다 "맞다."** 버그가 아니라 정의가 둘이다
    - 경로를 통일해도 반올림 규칙·자리수가 다르면 여전히 다르다 — 마지막 1% 가 소수점 한 자리
    - 여기에 타임존과 기준 컬럼이 겹치면 **같은 사건이 다른 날짜로 집계된다**
      (다른 저장소에서 독립 재발)
  - 15 는 동시성 때문에 틀리는 것이고 이쪽은 정의가 갈리는 것이다 — 겹치지 않는다

- [ ] **24. 구조체 정렬과 패딩** — 문서 기준 파서가 실제와 어긋난다
  - 출처: `event-stream-history.md` §2-2 (문서가 ★ 표시). 유형 F 계열
  - 필드 크기를 합해서 오프셋을 계산하면 컴파일러 정렬/패딩 때문에 밀린다
  - 반전 후보
    - 필드 합과 실제 구조체 크기가 다르다
    - **한 필드를 잘못 읽으면 그 뒤가 전부 밀린다** — 오류가 국소적이지 않다
    - 문서를 아무리 봐도 못 찾고 **덤프해서 오프셋을 역산**해야 찾는다
    - 버전마다 구조체 크기가 달라 결국 **크기로 레이아웃을 판별**하게 됐다
  - 손잡이: 필드 배치 · 정렬 규칙(1/2/4/8바이트) · 버전
  - 바이트 오프셋 자를 그리면 시각적으로 아주 선명하다. 다만 백엔드 독자층과 다소 거리가 있다

### 후보로 봤으나 약한 것 — 근거를 남겨둔다

- [ ] **상한(`limit N`)이 정확성 문제로 바뀐다** — `vwc-history.md` §2.3, 유형 B.
  1,001번째부터 조용히 안 보인다. 반전이 하나뿐이고("성능 문제를 정확성 문제로 맞바꿨다")
  11 의 논지와 겹친다. `prevention-automation.md` §2-H 가 이걸 🔴 로 평가해뒀다
- [ ] **200 OK 인데 실패다** — `sr-history.md` §5, 유형 F.
  HTTP 200 · `success:true` 는 전송 성공일 뿐인데 도메인 결과 필드를 안 봐서 무한 재시도가
  시작됐다. **원인** 쪽 재료인데 **결과**는 10·14 가 이미 다뤘다. 모델이 얇을 수 있다
- [ ] **회전 메타데이터를 네 경로가 각자 소비** — `blur-history.md` §1-1, 유형 G.
  문서상 **가장 오래 끈 항목**(1년 넘게 6회 재발)이고 처방도 명확하다(진입점에서 1회 정규화).
  다만 미디어 특화라 이 사이트 독자층과 거리가 있다
- [ ] **컴파일러는 절반만 잡는다** — 유형 I (4/6 재발).
  리네임·마이그레이션 때 XML 매퍼 · SQL 문자열 · 로그 인코딩 · yml 이 전부 통과한다.
  체크리스트형이라 손잡이가 안 나온다. 기존 페이지의 "현장에서" 표에 흡수하는 편이 낫다

> **재발 1위 유형은 실험대가 안 나온다.** 문서가 6/6 으로 1위로 꼽은 것은 유형 A
> "컴파일러가 검증하지 않는 문자열"(경로 조립 · 컬럼명 · 설정 오타 · 커맨드 옵션)인데,
> 이건 버그의 **분류**이지 메커니즘이 아니라 손잡이를 돌릴 대상이 없다.
> 하위 항목 중 유일하게 메커니즘인 "같은 상수가 세 곳에 독립 구현되어 한 곳만 고쳐졌다"
> (`event-stream-history.md` §1-2)는 21·24 의 프레이밍 이야기에 흡수하는 편이 낫다.

> **유형 J(크리덴셜 평문)는 후보에서 완전히 제외했다.** 아래 "소재를 옮길 때 지킬 것" 참고.

---

## 구조 (콘텐츠와 별개, 12편 시점에 필요)

- [x] 메인 페이지를 번호 나열에서 **시리즈 묶음**으로 바꾼다 (ko/en 양쪽)
  - 개념(01·02·03·04·13·16) / 실시간 통신(05·06·07) / 용량은 왜 무너지는가(08·11·14·17) /
    조용히 틀리는 것들(09·10·12·15·18). 19·20 도 용량으로 들어갔다. 번호는 그대로 두고 순서와 제목만 넣었다
  - 17 은 예고대로 **용량**, 18 은 **조용히 틀리는 것들** 로 들어갔다.
    18 은 그 그룹의 부제("예외도 로그도 없이 결과만 틀린다")가 레시피 3 문장 그대로다.
    네 그룹으로 계속 흡수된다 — 새 그룹을 만들지 않았다
- [x] 각 페이지 끝에 **"이어서 볼 것"** — 18편 × ko/en 36개 페이지에 넣었다 (2026-08-13)
  - 착수 전 실측: **18개 중 13개가 나가는 링크가 하나도 없는 막다른 길**이었다.
    링크가 있던 4개(14·15·16·17)는 전부 최근작이고, 그나마 단방향이었다
    (stampede→hashring 은 있는데 hashring→stampede 는 없었다)
  - 지금은 **링크 54개 · 들어오는 링크가 0인 페이지 0개.** 예고했던 05→14, 08→11→14,
    01→15, 13↔16(젠센), 09↔15 는 전부 들어갔다
  - 링크 맵은 `tools/related.mjs` 하나에 두고 `tools/gen-related.mjs` 가 HTML 에 박는다
    (본문 JS 주입 금지 규칙 때문에 생성해서 넣는다). 표식 사이에 들어가 **멱등**하고,
    링크 제목은 대상 페이지의 `<h1>` 에서 읽어와 제목을 고치면 문구가 따라온다
  - 고르는 기준은 **"실제로 이어져 있는데 화면에 없던 것"** 하나다. 한 줄의 이유가
    안 써지면 넣지 않았다 — "관련 글" 목록이 아니라 이유가 붙은 다음 걸음이다
  - `check-pages.mjs` 에 그래프 검사를 넣었다 — 자기 링크 · 중복 · 없는 페이지 ·
    빈 이유 · 막다른 길 · HTML 과 맵의 불일치를 잡는다. 변이를 넣어 실제로 잡는 것도 확인했다

  > **덤으로 잡은 것.** 01~13 은 본문을 `<article class="prose">` 로 감쌌는데
  > 14~18 은 그러지 않아서, `*{margin:0}` 리셋 때문에 **연속 문단의 여백이 0** 이었다.
  > 어떤 lab css 도 문단 여백을 주지 않으니 5개 페이지(ko/en 10개 파일)에서
  > 본문이 한 덩어리로 붙어 렌더되고 있었다. 실험대 안에는 prose 요소가 없어서
  > `<main class="wrap prose">` 로 안전하게 고쳤고, 같은 검사를 `check-pages.mjs` 에 넣었다.

### 소재를 옮길 때 지킬 것

- [ ] **익명화 필수** — 히스토리 문서에는 회사·제품명, 저장소명, 커밋 해시, 서버 경로
      (`/opt/dlami/nvme/data`), 내부 API 경로, 실제 테이블·컬럼명이 그대로 있다. 전부 일반화할 것
- [ ] **보안 부채는 소재에서 완전 제외** — `vwc` §5.2 / `vwc-media` §9 의 평문 커밋 크리덴셜
      (JWT 서명 secret, AWS 키, Apple `.p8`)은 아직 rotate 되지 않았다. 간접 언급도 하지 않는다

### 기존 페이지 보강

- [ ] `prevention-automation.md` §1-2 의 **발견 시점 비용 곡선**(타입 0 → 운영 3000)은
      그 자체로 작은 시각화 소재. 페이지 하단 공통 블록으로 쓸 수 있다

## 페이지 추가 체크리스트

새 시각화를 올릴 때마다:

- [ ] `public/<slug>/index.html` (ko) + `public/en/<slug>/index.html` (en)
- [ ] 실험대 로직/스타일은 `public/shared/<slug>-lab.js` · `<slug>-lab.css` 로 한 벌만
- [ ] 본문은 HTML 에 직접 — JS 주입 금지 (색인 안 됨)
- [ ] canonical + hreflang 3줄(ko·en·x-default), 양쪽 페이지 모두에 동일하게
- [ ] 인덱스 두 벌에 카드 추가 — 그룹은 `<details class="group">` 이므로
      `<h2 class="group">` 이 아니라 `<summary>` 안의 `<h2>` 를 앵커로 잡고,
      `<summary>` 의 `<span class="count">` 개수도 함께 올린다
- [ ] **"이어서 볼 것"** — `tools/related.mjs` 에 새 slug 의 나가는 링크를 넣고,
      **기존 페이지 몇 곳에서 새 페이지로 들어오는 링크도** 넣는다(안 넣으면 막다른 길이 된다).
      `NO` 에 번호도 추가. 그다음 `node tools/gen-related.mjs`
- [ ] **JSON-LD** — `node tools/gen-jsonld.mjs`. 링크 맵(`related.mjs`)의 slug 목록을
      그대로 쓰므로 위 항목을 먼저 하면 새 페이지가 자동으로 포함된다
- [ ] **검증을 돌린다** — `node tools/check-pages.mjs` (전 페이지 구동 · i18n 키 누락 ·
      태그 짝 · ko/en 키 집합 · 인덱스 카드 수 · 링크 그래프)와, 수치를 인용했다면
      `verify-<slug>.mjs`. `inject-beacon.mjs` **앞에서** 돌린다. 자세한 것은 `tools/README.md`
- [ ] `sitemap.xml` 은 **손으로 고치지 않는다** — `node gen-sitemap.mjs` 가
      각 페이지의 실제 마지막 커밋 날짜로 다시 만든다
- [ ] 배포는 이 순서로 — ① `gen-sitemap.mjs`(트리가 깨끗할 때) ② `inject-beacon.mjs`
      ③ `wrangler deploy`. 거꾸로 하면 beacon 주입 탓에 lastmod 가 전부 오늘이 된다
- [ ] 배포 후 Search Console 에서 sitemap 재제출 + URL 검사

## SEO

- [ ] `/mvcc/` 와 `/en/mvcc/` 가 각각 별도 색인되는지 확인
- [ ] `/gc/` 와 `/en/gc/` 동일 확인
- [ ] 색인 후 검색어별 노출 확인 — "VACUUM 테이블 안 줄어듦", "G1 ZGC 차이" 등

## 검토해볼 것

- [x] `public/404.html` 추가 — ko/en 한 장으로, noindex, sitemap 미포함
- [x] JSON-LD 추가 (2026-08-13) — `tools/gen-jsonld.mjs`, 40개 페이지

  > **`TechArticle` 로 안 했다.** 착수 전 Google 문서를 확인했더니 지원 타입은
  > `Article` · `NewsArticle` · `BlogPosting` 뿐이고 **`TechArticle` 은 그 목록에 없다.**
  > schema.org 상 Article 의 서브타입이라 틀리진 않지만 문서에 있는 것을 썼다.
  >
  > **실제로 값을 하는 건 `BreadcrumbList` 다** — 검색 결과에 URL 대신
  > `Backend Internals › 제목` 이 뜬다. Article 쪽은 기대를 낮게 잡아야 한다:
  > rich result 의 주된 이득이 큰 썸네일인데 **이 사이트에는 이미지가 하나도 없다**
  > (`og:image` 도 없다). 그래서 `image` 를 넣지 않았고, 이미지를 만들면 그때 추가한다.
  > 구조화 데이터는 순위에 직접 영향이 없다 — 표시·이해 장치다.
  >
  > `FAQPage` · `HowTo` 는 넣지 않았다. 갤러리에서 빠졌다(FAQ 는 정부·의료 사이트로
  > 한정, HowTo 는 폐지).
  >
  > `author` 에 **실명을 넣지 않고** 조직명으로 뒀다 — 사이트 어디에도 실명이 없으므로
  > 노출 여부는 판단이 필요하다. 넣을 거면 `gen-jsonld.mjs` 의 `org` 한 곳만 고치면 된다.
  >
  > 날짜는 손으로 안 적는다. `datePublished` = 첫 커밋, `dateModified` = 페이지와
  > 전용 `-lab.{js,css}` 중 마지막 커밋 (sitemap lastmod 와 같은 규칙).
  >
  > `check-pages.mjs` 에 검사를 넣었다 — 파싱 · `@context` · **페이지 내용과의 일치**
  > (`headline`=`<h1>`, `url`=canonical, `inLanguage`=`<html lang>`) · 날짜 형식과 순서 ·
  > 빵가루 `position` 연속성과 뿌리 URL. 변이 6종으로 잡히는 것을 확인했다.
  >
  > **덤으로 하니스 결함 하나를 고쳤다** — `boot()` 이 인라인 `<script>` 를 전부
  > 실행해서 `application/ld+json` 을 자바스크립트로 파싱하려 했다. `type` 을 보고
  > 자바스크립트만 실행하도록 바꿨다.
- [ ] 폰트를 CDN 대신 self-host 할지 (초기 렌더 지연)

## 연락처 노출

- [x] 푸터의 `mailto:` 평문 제거 — `shared/contact.js` 로 조립

- [ ] Cloudflare **Scrape Shield → Email Address Obfuscation** 켜져 있는지 확인
  - 대시보드 토글 하나. 켜면 Cloudflare 가 응답 HTML 의 `mailto:` 를 알아서 난독화한다
  - 다만 Workers/Pages 응답에도 적용되는지는 환경마다 다르다는 얘기가 있으니
    켠 뒤 실제 응답 소스를 직접 확인할 것. 위 JS 조립과 겹쳐도 해가 없다
- [ ] 더 갈 거라면 — 별칭 주소(`lab@`)를 따로 파서 푸터에만 쓰기.
  오염되면 별칭만 버리면 되고 본 주소는 안전하다
