# Backend Internals — TODO

> 이 폴더는 `public/` 밖이라 배포되지 않는다.
> `wrangler.toml` 의 `[assets] directory = "./public"` 이 업로드 범위를 정하므로,
> 여기 무엇을 넣든 사이트 URL 로는 접근되지 않는다.

## 콘텐츠

- [x] 03. 컨시스턴트 해싱 — 노드 하나가 빠지면 캐시는 어떻게 무너지는가
- [x] 04. Raft 리더 선출 — 네트워크가 갈라지면 누가 리더가 되는가
- [ ] mvcc 페이지의 "실제로 겪은 일" 주석 블록 채우기 (`public/mvcc/index.html`)
  - `docs/history/` 에 VACUUM 지연 사례는 **없다**. 원인의 형태가 같은 건 `bsd` §1-1
    (클래스 레벨 `@Transactional` 이 S3 업로드 구간까지 트랜잭션을 열어둠 — 장기 트랜잭션).
    증상만 풀 고갈로 달랐으니 그대로 옮기지 말고 각색할 것

### 실시간 통신 (인덱스 카드부터 추가해야 함 — 현재 01~04 가 전부 공개됨)

- [ ] MQTT QoS 0/1/2 + 패킷 손실
  - 네트워크가 불안정할 때 각 QoS 레벨에서 메시지가 어떻게 유실되고 중복되는지
  - QoS 2 의 4-way 핸드셰이크 비용을 QoS 1 의 중복과 나란히 놓고 비교
- [ ] WebSocket 다중 서버와 Redis Pub/Sub
  - 서버를 늘리면 다른 노드에 붙은 클라이언트에게 메시지가 안 가는 문제
  - Pub/Sub 로 풀 때 생기는 것들 — 팬아웃 비용, 유실(Pub/Sub 은 저장하지 않음), 재연결 중 공백
- [ ] Keep-alive와 죽은 커넥션 + LWT
  - TCP 는 살아 있다고 하는데 실제로는 끊긴 연결. keep-alive 주기와 탐지 지연의 관계
  - Last Will and Testament 가 언제 발행되고 언제 안 되는지 (정상 종료 vs 비정상 종료)

### 장애에서 나온 것들 (`docs/history/` 기반)

> 출처: 저장소 루트의 `docs/history/` 7개 문서 — 6개 저장소 · 약 2,500 커밋.
> 기존 01~04 가 교과서 개념이었다면 이쪽은 실제로 대가를 치른 것들이라
> "실제로 겪은 일" 블록 재료가 함께 나온다.
>
> 선정 근거는 `prevention-automation.md` §1-1 의 **"몇 개 저장소에서 독립 재발했나"** 숫자.
> 6/6, 5/6 짜리는 나만 겪는 일이 아니라는 뜻이다.
>
> 실시간 통신 시리즈와 축이 겹치지 않는다(DB · 동시성 · 배치). 둘을 나란히 진행한다.

- [ ] **커넥션 풀은 왜 17ms 쿼리 때문에 마르는가** — OSIV · 트랜잭션 경계
  - 출처: `bsd` §1-1 · §1-2, `vwc-media` §6-4, `vwc` §2.2 / 2개 저장소 독립 재발
  - 다이얼: 동시 요청 수 · 쿼리 시간 · `open-in-view` on/off · 트랜잭션 안에 외부 I/O(S3 업로드) 포함 여부
  - 보드: 커넥션 10개의 점유 타임라인. "빌린 시점 ~ 반납 시점" 이 막대로 칠해지고
    그 안에서 **실제 쿼리 구간은 아주 얇은 선**으로만 보인다
  - 반전: 누수 경고의 스택이 17ms 짜리 첫 쿼리를 가리킨다 (`ProxyLeakTask` 는 **빌린 시점**을 찍는다).
    OSIV 를 끄면 같은 부하에서 막대가 뚝 짧아진다
  - "현장에서 확인할 것" 재료가 이미 문서에 있다 — `pg_stat_activity` 진단 SQL,
    Hikari `leak-detection-threshold`, 런북
  - **1순위**

- [ ] **인스턴스를 2대로 늘리는 순간 같은 작업이 두 번 돈다** — 작업 선점
  - 출처: `blur` §3 · §5, `solution-review` §2-1 / 유형 D는 3/6 저장소
  - 다이얼: 인스턴스 수(1↔2↔3) · 선점 방식 4단
    (인메모리 `synchronized` → 상태값 read-then-write → 조건부 `UPDATE ... WHERE state='DOWNLOADED'`
    → `SELECT ... FOR UPDATE SKIP LOCKED`)
  - 보드: 작업 큐 행들과 워커들, **중복 실행 카운터**
  - 반전: 1대에서는 네 방식 모두 중복 0. 2대로 올리는 순간 앞의 두 방식만 카운터가 오른다
  - 4단 슬라이더가 `blur` 의 3회 시도 궤적 그대로다. booster 가 scale-out 을 목표로 하는 것과도 맞는다
  - **2순위**

- [ ] **롤백해도 시퀀스는 돌아오지 않는다** — 재시도 루프 폭주
  - 출처: `bsd` §2, `sr` §5 / 2건 모두 대형 장애
  - 다이얼: 실패 레코드 비율 · 재시도 주기 · 3요건 토글
    (시도 횟수 상한 / 영구 실패 상태 / 재시도 화이트리스트)
  - 보드: 시퀀스 게이지가 `2,147,483,647` 을 향해 오르고, 옆에 **실제 적재 레코드 수**는
    거의 멈춰 있다. 두 숫자가 벌어지는 것이 이 페이지의 전부
  - 반전: 영구 실패 상태 토글 하나만 켜도 게이지가 즉시 멈춘다.
    "검증은 부수효과보다 먼저" 를 순서 바꾸기로 확인
  - **3순위**

- [ ] **큐가 넘칠 때 무제한으로 여는 것의 대가** — 백프레셔
  - 출처: `blur` §2, `solution-review` §1-3 · §1-4
  - 다이얼: 생산 속도 · 소비 스레드 수 · 큐 정책(유계+Abort / 유계+CallerRuns / 무제한)
  - 보드: 큐 깊이 · 힙 · 거부 건수 · OOM 도달 시점
  - 반전: **거부는 버그가 아니라 신호다.** 무제한으로 열면 거부가 OOM 으로 바뀔 뿐
  - 같은 실험대에서 §1-4 도 다룰 수 있다 — 외부 프로세스를 Virtual Thread 로 띄우면 왜 나빠지는지

- [ ] **응답을 순서로 맞추면 언제 어긋나는가** — correlationId
  - 출처: `vwc` §1.1, `solution-review` §1-1
  - 다이얼: 동시 요청 수 · 응답 지연/순서 뒤집기 · 타임아웃 · 재전송(QoS 1 중복)
  - 반전: future 유실은 큐로 막히지만 **오매칭은 FIFO 로 못 막는다**.
    타임아웃으로 제거된 A 의 응답이 B 의 future 를 완료시킨다
  - **MQTT QoS 편과 같은 실험대에서 처리 가능** — QoS 1 재전송 중복이 곧 오매칭 시나리오다

### 소재를 옮길 때 지킬 것

- [ ] **익명화 필수** — 히스토리 문서에는 회사·제품명, 저장소명, 커밋 해시, 서버 경로
      (`/opt/dlami/nvme/data`), 내부 API 경로, 실제 테이블·컬럼명이 그대로 있다. 전부 일반화할 것
- [ ] **보안 부채는 소재에서 완전 제외** — `vwc` §5.2 / `vwc-media` §9 의 평문 커밋 크리덴셜
      (JWT 서명 secret, AWS 키, Apple `.p8`)은 아직 rotate 되지 않았다. 간접 언급도 하지 않는다

### 기존 페이지 보강

- [ ] `prevention-automation.md` §1-2 의 **발견 시점 비용 곡선**(타입 0 → 운영 3000)은
      그 자체로 작은 시각화 소재. 페이지 하단 공통 블록으로 쓸 수 있다

## 페이지 추가 체크리스트

새 시각화를 올릴 때마다:

- [ ] `public/<slug>/index.html` (ko) + `public/en/<slug>/index.html` (en)
- [ ] 실험대 로직/스타일은 `public/shared/<slug>-lab.js` · `<slug>-lab.css` 로 한 벌만
- [ ] 본문은 HTML 에 직접 — JS 주입 금지 (색인 안 됨)
- [ ] canonical + hreflang 3줄(ko·en·x-default), 양쪽 페이지 모두에 동일하게
- [ ] 인덱스 두 벌에 카드 추가
- [ ] `sitemap.xml` 에 ko/en 두 블록 (파일 안 주석에 템플릿 있음)
- [ ] 배포 후 Search Console 에서 sitemap 재제출 + URL 검사

## SEO

- [ ] `/mvcc/` 와 `/en/mvcc/` 가 각각 별도 색인되는지 확인
- [ ] `/gc/` 와 `/en/gc/` 동일 확인
- [ ] 색인 후 검색어별 노출 확인 — "VACUUM 테이블 안 줄어듦", "G1 ZGC 차이" 등

## 검토해볼 것

- [ ] `not_found_handling = "404-page"` 인데 `public/404.html` 이 없다. 현재는 평문 404 응답
- [ ] JSON-LD (`TechArticle`) 추가 여부 — 검색 결과 표시에 도움될 수 있음
- [ ] 폰트를 CDN 대신 self-host 할지 (초기 렌더 지연)
